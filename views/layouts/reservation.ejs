


<!DOCTYPE html>
<html>
  <body style="
    margin:0;
    padding:12px;
    font-family: Courier, Arial, Helvetica, sans-serif;
    background-color:#ffffff;
    color:#000000;
  ">

    <table width="100%" cellpadding="0" cellspacing="0">
      <tr>
        <td align="center">

          <!-- LOGO -->
          <img
            src="<%= logoUrl %>"
            alt="logo"
            width="120"
            style="display:block; margin:16px auto;"
          />

          <!-- GREETING -->
          <h1 style="
            margin:16px 0;
            font-size:22px;
            text-transform:uppercase;
          ">
            Salut, <%= reservation.client.name %>!
          </h1>

          <!-- STATUS -->
          <p style="font-size:16px; margin-bottom:20px;">
            Rezervarea cu numărul
            <strong><%= reservation.index %></strong>
            a fost
            <strong>
              <%= reservation.status === 'canceled'
                ? 'respinsă'
                : 'acceptată'
              %>
            </strong>.
          </p>

          <!-- DETAILS TITLE -->
          <h3 style="margin:16px 0;">Detaliile rezervării</h3>

          <!-- DETAILS TABLE -->
          <table
            width="100%"
            cellpadding="6"
            cellspacing="0"
            border="1"
            style="
              border-collapse:collapse;
              max-width:520px;
              margin:0 auto 20px auto;
              font-size:14px;
            "
          >
            <tr>
              <td>Nume</td>
              <td><strong><%= reservation.client.name %></strong></td>
            </tr>

            <tr>
              <td>Ziua</td>
              <td><strong><%= reservation.dateString %></strong></td>
            </tr>

            <tr>
              <td>Sosire</td>
              <td><strong><%= reservation.start %></strong></td>
            </tr>

            <tr>
              <td>Plecare</td>
              <td><strong><%= reservation.end %></strong></td>
            </tr>

            <tr>
              <td>Ședere</td>
              <td><strong><%= reservation.period %> ore</strong></td>
            </tr>

            <% if (reservation.kids > 0) { %>
              <tr>
                <td>Adulți</td>
                <td><strong><%= reservation.guests %></strong></td>
              </tr>
              <tr>
                <td>Copii</td>
                <td><strong><%= reservation.kids %></strong></td>
              </tr>
            <% } else { %>
              <tr>
                <td>Invitați</td>
                <td><strong><%= reservation.guests %></strong></td>
              </tr>
            <% } %>

            <tr>
              <td>Detalii</td>
              <td><strong><%= reservation.details || '-' %></strong></td>
            </tr>
          </table>

          <!-- LOCATION -->
          <p style="font-size:14px; margin-top:16px;">
            Ne găsești la
            <strong><%= reservation.salePoint.name %></strong>,<br/>
            <em><%= reservation.salePoint.address %></em>
          </p>

          <p style="margin-top: 8px;">
           <em> *Pentru informații de orientare va rugăm să ne contactați la <%=reservation.salePoint.telephone%> </em>
          </p>

          <p style="margin-top:12px;">
            Te așteptăm cu drag!
          </p>

        </td>
      </tr>
    </table>

  </body>
</html>
